{% extends 'base.html.twig' %}

{% block title %}Achat{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
    {% if msg == '' %}
                <p>
                    Féliciatation {{ dresseur.nom }} vous venez d'acheter un {{ pokemonDresseur.idPokemon.nom }} de niveau {{ niveau }} pour {{ vente.prix }} au dresseur {{ dresseurVendeur.nom }}.
                </p>

    {% else %}
            <p> {{ msg }} </p>
    {% endif %}
    </div>
                <div class="row">
                    <a href="{{ path('app_market_list') }}">
                        <button type="button" class="btn btn-primary">Revenir au marché des pokémons</button>
                    </a>
                </div>
    </div>
{% endblock %}
