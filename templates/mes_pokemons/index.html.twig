{% extends 'base.html.twig' %}

{% block title %}Hello MesPokemonsController!{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/mesPokemons.css') }}">
{% endblock %}
{% block body %}
<div class="container">
        <table class="table table-striped table-hover table-bordered">
            <tr>
                <td class="td-img">
                    Image
                </td>
                <td>
                    Nom
                </td>
                <td>
                    Surnom
                </td>
                <td>
                    Niveau
                </td>
            </tr>
        {% for pokemon in pokemonDresseur %}
                <tr>
                    <td class="text-center">
                        <a href="{{ path('app_modify_pokemon', {id:pokemon.id}) }}">
                        <img src="{{ asset( pokemon.idPokemon.photo ) }}" class="img-fluid"></img>
                        </a>
                    </td>
                    <td>
                        <a href="{{ path('app_modify_pokemon', {id:pokemon.id}) }}">
                        {{ pokemon.idPokemon.nom }}
                        </a>
                    </td>
                    <td>
                        {{ pokemon.surnom }}
                    </td>
                    <td>
                        {{ pokemon.getNiveau() }}
                    </td>
                    
                </tr>
        {% endfor %}
        </table>
</div>




{% endblock %}
